# `nuxi info`

```{bash}
npx nuxi info [rootDir]
```
`info`命令记录当前或指定Nuxt应用的信息。

Option        | Default          | Description
-------------------------|-----------------|------------------
`rootDir` | `.` | 目标文件夹
